# FHIR-52921: Clarify how derived profiles behave with respect to OperationDefinitions

## Metadata

- **__EMPTY**: Lloyd McKenzie
- **__EMPTY_1**: Public
- **__EMPTY_2**: Lloyd to propose language
- **Assignee**: Unassigned
- **Comments**: 11/17/25
- **Created**: 9/18/25
- **Description**: Normally a derived profile is allowed to be used in place of a parent profile.  However, for operations, there's a requirement that the profile used must align with the operation parameters.  This means certain types of changes in a derived profile are fine (e.g. changing descriptions), however other types of changes (messing with data types, adding parameters, changing cardinality) require a derived OperationDefinition.

Also make explicit what "SHALL NOT disagree" means - i.e. that the list of parameters and parts need to be identical, types need to be identical, cardinality needs to be identical, etc.
- **Issue Type**: Change Request
- **Key**: FHIR-52921
- **Reporter**: Lloyd McKenzie
- **Resolution**: Persuasive with Modification
- **Status**: Applied
- **Summary**: Clarify how derived profiles behave with respect to OperationDefinitions

- **Jira**: https://jira.hl7.org/browse/FHIR-52921

## Comments

### 1. Lloyd McKenzie - 11/17/25
Security: Public

Lloyd to propose language

### 2. Lloyd McKenzie - 11/17/25
Security: Public

Operation Parameter and Profile Alignment

There are two mechanisms used to define the rules for operation parameters - OperationDefinition.parameter and OperationDefinition.inputProfile and outputProfile.  The .parameter SHALL be used.  The inputProfile and outputProfile are optional and are used only when one or more of the input and/or output parameters conveys additional information (such as constraints, additional bindings, etc.)

When profiles are used, they SHALL be profiles on the Parameter resource (even if the expectation is that parameters will often be sent as part of the URL).  The elements must be aligned as follows:

	Parameters SHOULD be listed in the same order
	The number of input and output parameters must be the same
	The parameter names must be the same (parameter.name will be the fixed value for the `parameter.name` in the profile and SHOULD be the same as the parameter slice name)
	The parameter.min and max SHALL be the same as the min and max for the corresponding parameter slice
	If the parameter has a type and no list of allowed types, then the profile on parameter slice value SHALL either have the same type, or be one or more types that are derived from that type.
	If the parameter has a list of allowed types, then then the types of the corresponding parameter slice value SHALL correspond such that every allowed type is either listed explicitly as an allowed type or there are one or more types listed that derive from the operation parameter type.  There SHALL NOT be any types listed in the profile for 'value' that do not correspond to a type in the parameter
	All targetProfiles listed on the OperationDefinition for the parameter SHALL be listed as targetProfiles for at least one of the types for the value element in the corresponding parameter slice and there SHALL NOT be any targetProfiles listed that are not found in the OperationDefinition parameter
	The bindings SHALL be the same, though the profile may also have additionalBindings
	The .documentation SHOULD be the same as the .definition for the parameter slice
	If there are child parameters, then there should be aligned 'part' slices within the parameter following the same rules as above

