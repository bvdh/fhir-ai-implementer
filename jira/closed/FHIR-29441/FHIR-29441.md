# FHIR-29441: duplicate bullet in Binary

## Metadata

- **Assignee**: Unassigned
- **Created**: 10/26/20
- **Description**: first bullet is duplicated

 

	When a read request is made with a FHIR type in the Accept header (e.g. "application/fhir+xml" or "application/fhir+json") the Binary resource is returned in the requested FHIR format. This applies even when the binary data itself has a FHIR mime type
	When a read request is made with a FHIR type in the Accept header (e.g. "application/fhir+xml" or "application/fhir+json") the binary resource is returned in the requested FHIR format. This applies even when the binary data itself has a FHIR mime type
- **Issue Type**: Technical Correction
- **Key**: FHIR-29441
- **Reporter**: John Moehrke
- **Resolution**: Persuasive
- **Status**: Duplicate
- **Summary**: duplicate bullet in Binary

- **Jira**: https://jira.hl7.org/browse/FHIR-29441

## Comments

_No comments in export._
