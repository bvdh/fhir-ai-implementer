FHIR-55100: Apply planned wording corrections on exchanging

- Updated `conjuction` to `conjunction` in planned source scope
- Updated `doesn't` to `does not` in planned source scope
- Updated `isn't` to `is not` in planned source scope
- Applied 2 additional ticketed term correction(s) in target scope
