# FHIR-54484: Transform Functions issues (collection of them)

## Metadata

- **Assignee**: Unassigned
- **Created**: 1/16/26
- **Description**: Formatting for this section would be better as headings rather than a table, as these are more like functions, and would provide better ability to document each transform rule (function) better, and provide examples and links direct to the feature
	provide examples for all the transform functions
	all parameter types should be documented
	why do we have transform functions that exactly replicate a fhirpath function which can be used instead? i.e. truncate, translate, cast - when adding examples, include those as alternatives



 
escape transform 
  * parameter definitions?
  * fhirpath mapping - there are encode/decode functions in fhirpath
 
create transform - fhirpath equivalent is the %factory approach - and also new fhirpath create format that's the same as CQL already uses
 
create transform - when multiple versions of fhir are in play, is this the source or target type provider (think its target)?
 
cast transform - has an equivalent fhirpath expression - ofType(X) (or as)
  * how is this different to the source type casting? Is it even needed when there are group type rules that do this?
  * when multiple versions of fhir are in play, is this the source or target type provider (think its target)?
 
reference transform - what does that do, documentation doesn't describe anything, or provide an example
  * how does this compare to the `pointer` transform?
 
translate is in the transforms table multiple times
- **Issue Type**: Change Request
- **Key**: FHIR-54484
- **Related Issues**: FHIR-54485: toDate and toDateTime consider the fhir mapping language formatting codes for string parsing
- **Related Page(s)**: Mapping Language
- **Related URL**: https://hl7.org/fhir/6.0.0-ballot4/mapping-language.html
- **Reporter**: Brian Postlethwaite
- **Resolution**: Unresolved
- **Status**: Triaged
- **Summary**: Transform Functions issues (collection of them)

- **Jira**: https://jira.hl7.org/browse/FHIR-54484

## Comments

_No comments in export._
