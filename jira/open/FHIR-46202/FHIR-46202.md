# FHIR-46202: Correct regex in the documentation for code type

## Metadata

- **__EMPTY**: Bas van den Heuvel
- **__EMPTY_1**: Public
- **__EMPTY_2**: I deviated from the suggested regex:

For FHIR-46202, I implemented the code primitive regex as:

`^&#91;^\s&#93;( &#91;^\s&#93;)*$`

instead of:

`^&#91;^\s&#93;(\s&#91;^\s&#93;)*$`

Reason for deviation:

	The datatype prose says: "no whitespace other than single spaces in the contents".
	Using `\s` allows non-space whitespace characters (for example tabs and line breaks), which is broader than the prose requirement.
	Using a literal space in the separator enforces exactly one internal space between non-whitespace tokens, while still enforcing:
  - at least one character,
  - no leading/trailing whitespace,
  - no repeated internal spaces.



This keeps the regex behavior aligned with the normative wording for `code`.
- **Assignee**: Bas van den Heuvel
- **Comments**: 5/28/26
- **Created**: 6/21/24
- **Description**: The regex in the documentation page for code type is not correct. Here is the correct regex copied from fhir.schema.json:




^[^\s]+(\s[^\s]+)*$
- **Issue Type**: Technical Correction
- **Key**: FHIR-46202
- **Related Page(s)**: Datatypes
- **Related URL**: https://www.hl7.org/fhir/datatypes.html#code
- **Reporter**: AjayKanduru
- **Resolution**: Persuasive
- **Status**: Resolved - change required
- **Summary**: Correct regex in the documentation for code type
- **Work Group**: FHIR Infrastructure

- **Jira**: https://jira.hl7.org/browse/FHIR-46202

## Comments

### 1. Bas van den Heuvel - 5/28/26
Security: Public

I deviated from the suggested regex:

For FHIR-46202, I implemented the code primitive regex as:

`^&#91;^\s&#93;( &#91;^\s&#93;)*$`

instead of:

`^&#91;^\s&#93;(\s&#91;^\s&#93;)*$`

Reason for deviation:

	The datatype prose says: "no whitespace other than single spaces in the contents".
	Using `\s` allows non-space whitespace characters (for example tabs and line breaks), which is broader than the prose requirement.
	Using a literal space in the separator enforces exactly one internal space between non-whitespace tokens, while still enforcing:
  - at least one character,
  - no leading/trailing whitespace,
  - no repeated internal spaces.



This keeps the regex behavior aligned with the normative wording for `code`.

### 2. Bas van den Heuvel - 5/29/26
Security: Public

Jira removed some of the data: the regex is 

^&#91;^\s&#93;( &#91;^\s&#93;)*$

### 3. Bas van den Heuvel - 5/29/26
Security: Public

It does so again - see FHIR-46202: Correct code datatype regex documentation · HL7/fhir@2018c36 for the correct syntax.

