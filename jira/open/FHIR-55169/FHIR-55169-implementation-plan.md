# Implementation Plan: FHIR-55169

## Scope Summary
- Workgroup/source: Direct ticket scope from FHIR-55169 (FHIR Infrastructure)
- Tickets in scope: FHIR-55169
- Primary fix pattern: Structural binding correction for `ImplementationGuide.global.type` to use a cross-version resource type ValueSet

## Ticket Matrix
| Ticket | Summary | Status | Target page(s) | Change type | Notes |
|---|---|---|---|---|---|
| FHIR-55169 | ImplementationGuide.global.type has no escape valve | Resolved - change required | implementationguide.html (definitions for `ImplementationGuide.global.type`), value set documentation for version-independent resource types | Structural terminology binding update | Resolution indicates switching binding from `ResourceType` (`ValueSet/resource-types`) to cross-version `VersionIndependentResourceTypes` (`ValueSet/version-independent-resource-types`) |

## Likely Edit Surface (Files and Lines)
- `fhir-fork/source/implementationguide/structuredefinition-ImplementationGuide.xml:556` (`ImplementationGuide.global.type` element start)
- `fhir-fork/source/implementationguide/structuredefinition-ImplementationGuide.xml:569` (`elementdefinition-bindingName` currently `ResourceType`)
- `fhir-fork/source/implementationguide/structuredefinition-ImplementationGuide.xml:575` (binding description currently limited to "this version of FHIR")
- `fhir-fork/source/implementationguide/structuredefinition-ImplementationGuide.xml:576` (binding `valueSet` currently `http://hl7.org/fhir/ValueSet/resource-types`)
- `fhir-fork/source/terminologies/valueset-version-independent-resource-types.xml:17` (canonical URL to target)
- `fhir-fork/source/terminologies/valueset-version-independent-resource-types.xml:22` (ValueSet name `VersionIndependentResourceTypes`)
- `fhir-fork/source/fhir.ini:1300` (registry mapping for `version-independent-resource-types`, verify only)

## Shared Implementation Approach
1. Locate the `ImplementationGuide.global.type` binding definition in `structuredefinition-ImplementationGuide.xml`.
2. Replace the bound ValueSet URI with the cross-version ValueSet used for version-independent resource typing.
3. Align binding narrative text and binding name only as needed to remain consistent with the selected ValueSet and ticket resolution.
4. Keep edits minimal and constrained to `fhir-fork/source/`.

## Execution Steps
1. Open `fhir-fork/source/implementationguide/structuredefinition-ImplementationGuide.xml` and navigate to the `ImplementationGuide.global.type` binding block.
2. Update `<valueSet value="http://hl7.org/fhir/ValueSet/resource-types"/>` to `<valueSet value="http://hl7.org/fhir/ValueSet/version-independent-resource-types"/>`.
3. Review and, if needed, update adjacent binding metadata for consistency:
   - `elementdefinition-bindingName` (currently `ResourceType`) only if naming conventions require matching the new binding semantics.
   - Binding `description` text so it no longer implies only current-version resource types.
4. Confirm no additional hand-edited changes are needed in generated diagram files (`implementationguide.svg` / `implementationguide.gen.svg`); if those artifacts are generated by the standard build flow, defer regeneration to normal publication/build process rather than manual edits.
5. Run a focused diff to verify only intended lines changed in `structuredefinition-ImplementationGuide.xml`.
6. Perform a consistency spot-check for this ValueSet usage pattern in other resource definitions (for example, `basic/structuredefinition-Basic.xml`) to ensure terminology usage is aligned with existing repo conventions.

## Validation Checklist
- [ ] FHIR-55169 is mapped to at least one concrete source file and line block.
- [ ] Planned edits stay within `fhir-fork/source/`.
- [ ] `ImplementationGuide.global.type` binding points to `http://hl7.org/fhir/ValueSet/version-independent-resource-types`.
- [ ] Binding narrative text is consistent with cross-version semantics and ticket resolution intent.
- [ ] No unrelated formatting/tooling/build file changes are introduced.
- [ ] Plan remains traceable to ticket resolution: "change binding ... to CommonResourceTypes (cross-version by nature)".

## Risks and Assumptions
- Risk: Changing only the ValueSet URI but leaving overly narrow description/binding labels may create semantic inconsistency in published definitions.
- Risk: If local workflow expects regenerated artifacts to be committed, diagrams or derived pages may appear stale until generation is run.
- Assumption: `ValueSet/version-independent-resource-types` is the intended concrete target corresponding to the resolution's "CommonResourceTypes" wording.
- Assumption: No additional constraints in `ImplementationGuide.global.profile` or related invariants require adjustment for this ticket.
- Open questions: Confirm whether WG expects any explicit rename of binding display/name text (e.g., keeping `ResourceType` vs adopting a version-independent term) beyond the ValueSet URI update.
